# Python testing dependencies project

This project is a sample Python application with intentionally outdated dependencies. It is used to test dependency monitoring systems for detecting outdated or vulnerable packages.

## Setup

To install dependencies, run:

```bash
pip install -r requirements.txt
```

## Running the Application

To run the main application logic:

```bash
python app/main.py
```
